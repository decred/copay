FROM node:6.11.3

LABEL description="BWS (Decred fork)"
LABEL version="1.0"
LABEL maintainer "peter@prioritylane.com"

USER root
WORKDIR /root

COPY ./ /root/

RUN npm install

CMD npm build && chmod -R a+rw build
